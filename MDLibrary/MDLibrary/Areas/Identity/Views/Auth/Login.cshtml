@model LoginViewModel

@{
	ViewData["Title"] = "Войти";
}

<section class="vh-100">
    <div>
        <div class="row">
            <div class="text-danger" asp-validation-summary="ModelOnly"></div>
            <div class="col-sm-6 text-black">

                <div class="px-5 ms-xl-4 mb-3">
                    <i class="fas fa-flask fa-2x me-3 pt-5 mt-xl-4" style="color: #709085;"></i>
                    <span class="h1 fw-bold mb-0">МИБС</span>
                </div>

                <div class="d-flex align-items-center px-5 ms-xl-4 pt-2 pt-xl-0 mt-xl-n5">

                    <form asp-area="Identity" asp-controller="Auth" asp-action="Login" method="post" style="width: 23rem;">

                        <h3 class="fw-normal mb-3 pb-3" style="letter-spacing: 1px;">Вход</h3>

                        <div class="form-outline mb-4">
                            <input asp-for="@Model.Username" id="form2Example18" class="form-control form-control-lg" />
                            <label class="form-label" for="form2Example18">Имя пользователя</label>
                            <span class="text-danger" asp-validation-for="@Model.Username"></span>
                        </div>

                        <div class="form-outline mb-4">
                            <input asp-for="@Model.Password" type="password" id="form2Example28" class="form-control form-control-lg" />
                            <label class="form-label" for="form2Example28">Пароль</label>
                            <span class="text-danger" asp-validation-for="@Model.Password"></span>
                        </div>

                        <div class="form-check">
                            <input asp-for="@Model.RememberMe" class="form-check-input" type="checkbox" id="flexCheckChecked" checked>
                            <label class="form-check-label" for="flexCheckChecked">
                                Запомнить меня
                            </label>
                        </div>

                        <div class="pt-1 mb-4">
                            <button type="submit" class="btn btn-info btn-block">Войти</button>
                        </div>

                        <p class="small mb-2 pb-lg-2"><a class="text-muted" href="#!">Забыли пароль?</a></p>
                        <p>Ещё нет аккаунта? <a href="#!" class="link-info">Регистрация</a></p>

                    </form>

                </div>

            </div>
            <div class="col-sm-6 px-0 d-none d-sm-block">
                <img src="/images/login-page.jpg"
                     alt="Login image" class="w-100 vh-100" style="object-fit: cover; object-position: left;">
            </div>
        </div>
    </div>
</section>